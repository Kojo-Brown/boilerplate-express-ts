# Spec: boilerplate-express-ts

> Spec-driven. Mark `[x]` only after pushing.

## Phase 0 — Green Baseline (blocks all feature work)
- [x] Verify every dependency version actually exists on the registry and fix the ones that do not, then commit a lockfile
- [x] Get `install`, `typecheck`, `lint`, `test`, and `build` all passing locally from a clean clone
- [x] Promote `workflow-templates/ci.yml` to `.github/workflows/ci.yml` and confirm it runs green on a PR
- [x] Add a CI job matrix covering the supported Node version and fail the build on any warning — `engines.node` narrowed to `^22.12.0 || ^24.0.0`; lint, typecheck, test, and build run on both majors with `--strict-peer-dependencies`, `--max-warnings=0`, and `NODE_OPTIONS=--throw-deprecation` (PR #22)

## Phase 1 — Foundation
- [x] Express 5 + TypeScript 6 scaffold with strict tsconfig and `@/` path alias
- [x] Zod-validated env vars + config module
- [x] Router factory pattern: versioned `/v1/` routes via `express.Router()`
- [x] Global error handler middleware with typed `AppError` class
- [x] Request logger middleware (Morgan + correlation ID)

## Phase 2 — Auth
- [x] JWT access + refresh token flow (jsonwebtoken)
- [x] OAuth 2.0 PKCE flow with Google (passport + passport-google-oauth20)
- [x] Argon2 password hashing
- [x] Rate limiter middleware (`express-rate-limit`) on auth routes
- [x] Auth middleware factory: `requireAuth`, `requireRole`

## Phase 3 — Database
- [x] PostgreSQL connection pool via `pg` with typed query helper
- [x] SQL migration runner (node-pg-migrate)
- [x] Repository pattern: typed CRUD base class
- [x] Transaction helper with automatic rollback

## Phase 4 — API Patterns
- [x] Zod request validation middleware (body, query, params)
- [x] Cursor-based pagination helper
- [x] File upload (Multer + S3 presigned URLs)
- [x] Response envelope: `{ data, meta, error }`

## Phase 5 — Testing & DevOps
- [x] Jest + Supertest E2E tests for auth + CRUD
- [x] GitHub Actions: lint → typecheck → test → build → Docker push
- [x] Multi-stage Dockerfile + docker-compose with postgres

## Phase 6 — SOLID & Design Patterns
- [x] SOLID audit with before/after refactors documented in `docs/solid.md` — three of the five findings were live defects, not style: a duplicate email returned 500 because the error middleware was a closed `instanceof` ladder (and `upload.router.ts` had already grown a parallel Multer handler to work around that closure), `findByRole` cast an array through the base class's equality `WHERE` so the seeded admin never matched, and the controllers' hand-rolled 422 discarded Zod's `issues` while `validate.middleware.ts` sat unused (PR #23)
- [x] Factory + Registry: `ProviderRegistry` resolving adapters by key with compile-time exhaustiveness — `TKey` pinned to a union rather than inferred, so the table is checked both ways: a key with no factory fails the `Record`, a factory outside the union is an excess property, and both errors land at the registration site (PR #24)
- [x] Strategy pattern: swappable `AuthStrategy` (password, magic link, API key) — every strategy resolves an `AuthenticatedPrincipal` and `AuthService` mints the pair from it, so nothing after login branches on how the caller authenticated. The credential type is erased in exactly one place (`defineAuthStrategy`), because a heterogeneous registry of `AuthStrategy<TCredentials>` collapses to a union whose `authenticate` takes the *intersection* of three unrelated shapes; `/login/:strategy` therefore validates inside the strategy rather than at the router, which is a deliberate departure from the edge-validation convention. Tokens and API keys are stored as SHA-256 digests, not argon2 — a KDF buys nothing against 256 bits of CSPRNG entropy. Both env-dependent defaults fail closed: magic-link delivery refuses to send in production until a transport is wired, and the published `mock-api-key-*` dev keys seed only outside production (PR #25)
- [x] Decorator pattern: higher-order route handlers `withRetry`, `withTimeout`, `withCache` — they wrap a `RouteOperation` (request in, value out), not `(req, res, next)`: once a handler has called `res.json()` a retry has nothing to retry into and a cache has no return value to store, so the naive signature can only support one of the three. `toRequestHandler` is the single file that touches `Response`. What each one *refuses* to do carries the design — `withRetry` runs POST/PATCH exactly once and backs off with full jitter; `withTimeout` aborts the operation's signal with the `TimeoutError` as the reason rather than merely ceasing to wait, so the pooled connection is actually released; `withCache` keys on the principal (omitting it is a cross-account leak on `GET /v1/users`), never caches failures, coalesces concurrent misses, and stores into a bounded LRU because URL-derived keys make an unbounded map a memory-exhaustion primitive. Wired into the users routes for real — reads run cache → retry → per-attempt timeout, writes clear the store and deliberately get no retry, since a delete that commits and then loses the connection would answer 404 on the replay (PR #26)
- [x] Observer pattern: typed `EventBus` on Node `EventEmitter` with domain events — the emitter is the right primitive and the wrong interface, and each gap is a production failure rather than a style complaint: `emit('user.delted', …)` typechecks and silently never fires, `emit` discards an async listener's promise so a rejection takes the process down, and a listener that throws throws *out of `emit`* into whoever published. Dispatch still goes through `emitter.emit` — `publish` passes a collector alongside the envelope and each wrapper pushes its isolated promise into it — because snapshotting `listeners()` and calling them by hand loses `once` removal, listener counts and leak warnings. `error`/`newListener`/`removeListener` are refused outright, since emitting `error` with no listener throws into the publisher, the exact coupling this breaks. Wired for real: `DELETE /v1/users/:id` publishes `user.deleted` and a subscriber revokes that user's refresh tokens, which previously stayed valid for a week after the account was gone — and the users module still imports nothing from auth. The audit subscriber's descriptor table is exhaustive over the event union, so a new event fails to compile until someone decides what it means for the audit trail. Two omissions carry the design: cache invalidation stays inline in the controller (isolating a failed invalidation means serving the row you just changed for the rest of the TTL — events carry the consequences a publisher can afford to lose), and rotation publishes nothing, because the signal worth having from that path is a *reused* refresh token, which is Phase 10's event, not this one (PR #27)
- [x] Singleton lifecycle: a hand-rolled DI container with singleton/scoped/transient registration — the module-level `export const userRepository = new UserRepository()` it replaces was *already* a singleton, so the instance count is unchanged; what changes is that the lifetime became a decision and the wrong combination became a loud error. The captive dependency is that error: a singleton holding a per-request object serves the first request's principal to every later one, and no single-request test can see it. Singleton factories therefore resolve against the root, where scoped tokens do not exist, so the failure lands at the first resolution with the whole path named — including through an intermediate transient, since the path is carried through the entire resolution rather than checked one edge at a time. Three decisions live in the API rather than the docs: factories are synchronous (an async one lets two concurrent resolutions both enter a singleton's factory, making it "usually one"), `registerTransient` has no `dispose` option at all (holding a reference so it could be closed later makes every resolve a leak), and re-registering a token throws (last-write-wins is how a test stub reaches production). Wired in for real — one scope per request, disposed on `close` rather than `finish` so a client that hangs up does not strand it, and `users.controller.ts` resolves its repository, the bus and a scoped `RequestContext` from that scope. `RequestContext` reads the request through accessors, not a snapshot, because a scoped instance is built at first *resolve*, possibly before `requireAuth` has run. Nothing is registered transient and that is a finding, not a gap: every collaborator here is stateless or per-request (PR #28)
- [x] Middleware composition: a typed `compose()` pipeline replacing ad-hoc `next()` chains — a middleware array is an ordered list matched against a path, and the three things it cannot express had all become comments in this repo. Ordering ("auth stays ahead of validation so an unauthenticated caller learns nothing about the accepted request shape") was enforced by nothing; `req.auth` stayed `JwtPayload | undefined` inside handlers unreachable without a token, because the global augmentation describes every request in the process; and `toRequestHandler`'s cast to the operation's own `TReq` was justified by a `validate()` call in a different file. A step takes the request and returns it refined, so `TIn` sits in a parameter position and `strictFunctionTypes` makes `compose().use(requireRoles('admin'))` a compile error rather than a 401 that answers admins and impostors alike. There is no `next` — it overloads continue, fail and skip-this-route onto one callback that Express can neither disambiguate nor call once. What it refuses carries the rest: `fromRequestHandler` exists because `express-rate-limit`, `multer` and `passport` never will be steps and its type says it proves nothing, but it ignores a second `next()`, resolves when middleware answers without calling `next` at all, forwards a rejected promise Express 5 would have caught, and rejects `next('route')` outright since a composed pipeline has no remaining stack to abandon. Two defects surfaced on the way: `req.query = parsed` throws on a real Express 5 request (a getter with no setter), which the unit tests had hidden by building `req` as an object literal, and `createUser` declared params as `Record<string, string>` where Express 5 types a parameter as `string | string[]` (PR #29)

## Phase 7 — Concurrency & Data Integrity
- [x] Idempotency middleware: `Idempotency-Key` + Postgres dedupe table with response replay — the record is taken at the *response* boundary rather than around the handler, because a decorator in the `withCache` family sees `TResult` and what has to be replayed is the serialised response: `created_at` is a `Date` going out and a string coming back, so a store typed by the operation's return value would claim a shape it cannot produce, and nothing on the value side would catch it. It wraps `res.json`/`res.end` and persists *before* flushing — `res.on('finish')` never fires for a client that disconnects mid-flush, which is exactly the client about to retry. It is a pipeline step, not a `(req, res, next)` middleware, because keys are client-generated strings and the table is therefore scoped by principal: mounted ahead of `authenticate` every caller shares the `anonymous` scope and replays someone else's body, so the ordering is a compile error instead of a comment. `INSERT ... ON CONFLICT DO NOTHING` is the claim — one round trip, and unlike SELECT-then-INSERT it cannot succeed for two callers at once — with every timestamp compared by Postgres and `claim_id` fencing `complete`/`release` so a superseded request cannot overwrite its successor's record. 4xx is recorded (a deterministic rejection is the answer), 5xx releases the claim, and 408/425/429 are never recorded, since freezing "not now" into a day-long window keeps answering 429 from a limiter that has relented. `POST /v1/users` now requires the header — a deliberate breaking change; the existing e2e cases gained a key and lost no assertion. What is *not* claimed: the claim row and the work commit separately, so a crash between them costs one re-execution — closing that needs the record inside the write's own transaction, which is the outbox item below. Verified beyond the suite against a real PostgreSQL 16: migration applied, rolled back and re-applied, and 20 concurrent claims on one key producing exactly one winner (PR #30)
- [x] Optimistic concurrency with a `version` column + `If-Match`/ETag, 412 on mismatch — the version is bumped by a `BEFORE UPDATE` trigger rather than by the repository's own `SET` clause, because a validator that only holds when the writer remembers to opt in is not a validator: `BaseRepository.update`, a backfill and a `psql` session are all writers, and any of them moving the row without moving the version leaves an already-issued `ETag` comparing equal to a state it no longer describes. That is also what keeps the inherited unconditional writes safe to expose. The check is in the `WHERE` clause, not in a read before it — read-then-write reintroduces the race at the point of fixing it — and it is one statement, not two, because an `UPDATE` affecting no rows cannot say whether the row is gone (404) or at another version (412), and asking afterwards lets a concurrent delete turn a genuine conflict into the one answer that tells a client to stop retrying; both branches share a snapshot via a data-modifying CTE. `If-Match` is required rather than optional (428 without it), since a route that protects only the careful clients offers a guarantee no client can detect — and the usual cost is not charged, because `If-Match: *` is a first-class answer, so what is required is that the expectation be *stated*, not that it be narrow. The parser rejects what a client must change (a non-list header, a weak tag — `If-Match` is strong comparison, so `W/"7"` could never match and a 412 would loop forever) and merely drops what simply does not match (`"abc"`, `"007"`, anything past `integer` range), which becomes a 412; the list is scanned rather than `split(',')`, since a comma is a legal `etagc`. `requireIfMatch` refines the request type, so a router that drops it does not compile. Two things fell out: `AppError` can now carry response headers — a `RouteOperation` never sees a `Response`, and the `ETag` on a 412 is the half of that answer a client acts on — and a conflict clears this replica's cached reads, or the client's recovery `GET` returns the same stale validator and its retry conflicts again for the length of the TTL. Verified beyond the suite against a real PostgreSQL 16: the migration applied, rolled back and re-applied; the trigger bumped the version for a hand-written `UPDATE` and overrode one that tried to set `version` itself; and 20 concurrent writers naming the same version produced exactly one winner, 19 conflicts, and a row that advanced by exactly one (PR #31)
- [x] Pessimistic locking with `SELECT ... FOR UPDATE` and a deadlock-retry wrapper — the version column from #31 is a compare-and-swap over one row and is complete for a lost update; it cannot express a rule over a *set*. Two requests demoting two different administrators leave each other's rows untouched, so both `If-Match` preconditions pass, both writes are individually correct, and the pair leaves nobody able to administer the system. Deciding something about a set means holding the set still, so `PUT` and `DELETE /v1/users/:id` now lock every administrator before checking, and refuse the last one with 409 `LAST_ADMIN`. Four decisions carry it. The lock cannot be taken outside a transaction — `FOR UPDATE` on an autocommit connection takes the lock and releases it at the end of the statement, silently, leaving the read-modify-write exactly as racy as before, so the helpers take a symbol-branded `TransactionClient` and passing the pool is a compile error rather than a race under load. The set is locked in primary-key order and unconditionally, including on the many deletes whose target is not an administrator, because the cheaper "lock the target first, reach for the set only if needed" shape puts back the exact cycle the ordering removes. `FOR NO KEY UPDATE` for the role patch and `FOR UPDATE` for the delete, since a foreign-key check takes `FOR KEY SHARE` on the parent and only the delete changes a key. And only database work goes inside the transaction — the retry may run the callback twice, and a rollback takes the writes back but not a published event or a cleared cache. The two timeouts are the subtle part: `lock_timeout` (750ms) bounds the wait because a blocked statement does not notice the route's deadline and holds its pooled connection after the request is already answered, and `deadlock_timeout` (250ms) has to sit *under* it or the retry loop is unreachable — at the server's 1s default a genuine cycle is reported as `55P03` before the detector runs, and only `40P01` is retried. The wrapper refuses more than it accepts: `40P01` and `40001` are safe to replay because Postgres has already rolled the transaction back on a healthy connection, `55P03` is the caller's own timeout and retrying it is a busy-wait, and connection loss is never retried because a connection that dies with `COMMIT` in flight leaves the outcome unknown. Verified beyond the suite against a real PostgreSQL 16: eight concurrent demotions of eight administrators end with seven successes, one 409 and exactly one administrator left; `lock_timeout` aborts a waiter at the configured time and `NOWAIT` at once; a deliberately crossed lock order deadlocks and the loser retries to success, while the same interleaving without the wrapper surfaces a raw `40P01` — and six concurrent *ordered* set locks produce none at all (PR #32)
- [x] Distributed lock via Postgres advisory locks with automatic release — a row lock can hold still anything that exists in a table, which is also its limit: the things a multi-replica service needs to serialise are *jobs*, and a job has no row. Inventing a `jobs` table so there is something to `FOR UPDATE` buys a migration and a cleanup story for a mutex Postgres already offers. The transaction-scoped family is the whole point of the item's last three words — `pg_advisory_xact_lock` is released by the transaction ending, on `COMMIT`, on `ROLLBACK`, and on the connection dying with neither, so there is no unlock call and no `finally` that could be skipped. Every other distributed lock needs a lease, a heartbeat and a fencing token to survive a holder disappearing; Postgres already knows the holder is gone, because the holder is a session. The helpers therefore take a `TransactionClient` and the brand is load-bearing: on an autocommit connection the same call takes the lock and drops it at the end of that statement, silently, leaving the critical section unprotected — the same no-op `FOR UPDATE` produces there. `try`, not `wait`, is the default for scheduled work, since queued losers drain into N consecutive sweeps of a table the first one already emptied. `withAdvisorySessionLock` exists for work too long to sit in a transaction — an open transaction holds back the cluster's `xmin` horizon, so a job that only wanted a mutex stops autovacuum database-wide — and there the release has to be *engineered*: a pooled connection's session never ends, so an unlock that returns `false` or throws destroys the connection instead of returning it, because ending the session is what Postgres releases a session lock on. Keys use the two-`int4` form under a namespace rather than the `bigint` form: they are separate lock spaces, and the space is global to the database and shared with everything else in it (`node-pg-migrate` takes one of its own). The SHA-256 derivation is pinned by a test because two replicas exclude each other only by computing the same integers — a deployed contract, not an implementation detail — while a collision *inside* a namespace is a false conflict and never a missed one, which is the argument for a few named coordination points rather than a key per row. Wired into a live defect: `idempotency_keys` was append-only in practice, `purgeExpired` having been called by nothing but its own unit test since the record was introduced, so every guarded POST left a row behind for the life of the deployment. `purgeExpired` gained the optional `Queryable` every database-backed method here takes, so the `DELETE` runs inside the locked transaction rather than on another connection outside it, and the job lives in `server.ts` rather than `createApp()` because a timer belongs to the process and every e2e suite builds an app. Verified beyond the suite against a real PostgreSQL 16.13: `lock_timeout` aborts a waiting advisory acquisition (403ms at a 400ms setting, `55P03`); a crossed lock order across two advisory keys deadlocks as `40P01` and `withRetryableTransaction` recovers on the second attempt; the `bigint` and two-`int4` forms do not conflict; the xact lock is released by both `COMMIT` and `ROLLBACK` with no unlock issued; one of 20 concurrent try-lock holders wins; a session lock survives a `COMMIT` on its own connection, is released after its callback throws, and its wait is bounded by `lock_timeout`; and eight racing sweeps never overlap in the critical section while deleting the 25 seeded rows exactly once between them. Not claimed: the destroy-on-failed-unlock path is unit-tested only, and CI still exercises none of this against its Postgres service — `jest.setup.ts` overrides `DATABASE_URL` unconditionally, which predates this item (PR #33)
- [x] `worker_threads` pool for CPU-bound work with a bounded queue and graceful drain — the first work here the event loop cannot absorb: `createHash().update()` is synchronous C++ with no yield point, so a 64 MiB upload is ~40ms during which the process answers nothing, and under concurrency those stalls queue rather than interleave — p99 on every *unrelated* endpoint degrades in proportion to upload traffic, and no amount of `async` helps because there is nothing to await. The queue is bounded because an unbounded one does not remove a limit, it hides one: threads are the real constraint, so the overflow grows memory without limit (the payloads are file-sized, which is *why* the work is CPU-bound) and latency with it, until the pool is spending real CPU on answers for clients that already left. It becomes 503 + `Retry-After` in microseconds instead — 503 and not 429, since 429 says *you* sent too much and would penalise a well-behaved caller for load somebody else generated — and the bound counts tasks *waiting*, not outstanding, because work on a thread is progressing. A timeout destroys the thread, because a synchronous CPU task cannot be cancelled: there is no point at which an `AbortSignal` could be read, and `Promise.race` would abandon the result while the thread stayed pinned forever, losing a worker per timeout. Termination is not instant either — `terminate()` stops the isolate at a *JavaScript* boundary and cannot interrupt a call already inside OpenSSL — so between the timeout and the exit the process runs more than `size` threads; asserted against a real thread rather than assumed, and the reason `taskTimeoutMs` is a backstop rather than a deadline. `drain()` finishes the queue where `terminate()` discards it, which is the distinction that matters during a deploy: a queued task is a request whose client is still waiting, and dropping it produces exactly the burst of 503s the rolling deploy existed to avoid. Two smaller things carry weight: workers are `unref`'d and the per-task timer is not, so an idle pool never holds the process open and a pool with a task in flight always does (unref'ing both would let the process exit out from under an `await pool.run()`); and the worker's whole module graph uses relative imports, because a thread entry is named by a path on disk and inherits no resolver — jest's `moduleNameMapper` cannot reach it and `tsc` rewrites nothing on emit. `resolveWorkerEntry` derives the extension from `__filename` and injects a ts-node hook only under jest. Wired into a real consumer rather than left for a later one: `POST /v1/uploads` returns a SHA-256 of exactly the bytes it stored, on a thread above 64 KiB and inline below — the threshold being the judgement written down, and the reason none of the existing hashing (`pkce`, `secret-hash`, `advisory-lock`, the idempotency fingerprint) moved, since a round trip costs more than hashing a few bytes. Nothing is transferred: Multer's sub-8 KiB buffers are slices of a shared pool, so transferring one detaches unrelated live buffers. Writing the tests found one real bug — `handlers[task]` with a `typeof` check would have dispatched a task named `constructor` into `Object`. Verified beyond the suite on **both** Node 22.22.2 and 24.11.0 (ts-node loading a `.ts` entry inside a worker was the part most likely to differ across majors): all five gates green under `--throw-deprecation`, 1062 tests up from 954, and the compiled `dist/workers/cpu.worker.js` run in a real thread under plain `node` — no ts-node, no jest, no alias resolver — returning a digest matching the main thread. Not claimed: `AbortSignal` is not plumbed into `run()` (a queued task could be cancelled cheaply and a running one not at all, and half a cancellation contract is worse than none); idle threads are never reaped; disposal is not wired to a signal handler, which is the graceful-shutdown item's to own. Found while verifying the build and deliberately left alone as out of scope: `node dist/server.js` cannot run at all — `tsc` performs no path rewriting, so every `@/…` lands verbatim in `dist/` and `pnpm build` only compiles, which is why this worker's graph avoids the alias and is currently the only part of `dist/` that executes (PR #34)
- [x] Immutability: `readonly` types, `Object.freeze` in dev, pure update helpers — the hazard is not style, it is that `MemoryCacheStore` stores the object it is given: it neither serialises nor copies, which is what makes it cheap enough to sit in front of `GET /v1/users`, so the array handed to the request that missed is the array handed to the next hundred that hit. `users.sort(byCreatedAt)` in a handler is then an edit to the cache — answered from the reordered copy for the rest of the TTL, then healed by expiry, which is what makes the report ("the list is sometimes in the wrong order") impossible to attach to the line that caused it. Nothing about that line is wrong on its own and the type agrees: the operation returns `UserRow[]`. Two other sites have the same shape — `publish` fans one envelope out to subscribers running concurrently on the same reference, and `env` is imported by fifteen modules that all treat it as a constant. Three mechanisms, because each covers the others' gap: `DeepReadonly<T>` reports at the call site and costs nothing at runtime, but has a hole worth naming — TypeScript ignores `readonly` property modifiers in assignability, so a handler annotated with the mutable type is accepted where the readonly one is expected (which is why the type test compares deferred conditionals rather than `extends` both ways, and why the audit descriptors spell the readonly view out); `deepFreeze` cannot be argued with, since `alwaysStrict` follows from `strict` and a write throws at the offending line; `freezeInDev` is the compromise, and its return type stays `T` because widening it to `DeepReadonly<T>` would make a decorator's static contract depend on `NODE_ENV`. What the freeze walks *past* carries as much as what it freezes: `Object.freeze` does nothing to a `Date` or a `Map` — internal slots — and **throws** on a typed array, so a `Buffer` in a cached response would have turned a dev-only check into a dev-only crash; `DeepReadonly` maps `Map`/`Set` to their readonly forms, which is the layer that holds for them. Accessors are skipped rather than read, a `WeakSet` handles cycles and diamonds, and leaves are matched by `Object.prototype.toString` because a value cloned back from a worker thread was built in that thread's realm. Wired where the sharing is: `env` frozen unconditionally and typed `DeepReadonly` (one object, once, at boot); `MemoryCacheStore.set` in dev, freezing the *current* caller's object since that is the one most likely to mutate it; `publish` in dev, with `on`/`once` now delivering `SubscriberView<TPayload>` — producers write, consumers read. The helpers are three and no more: spread is already the answer for the common case, `patch` exists only because it returns the same reference when nothing changed (a no-op `PUT` otherwise defeats every `===` downstream), and `omit`/`pick` because the hand-rolled forms must widen to a `Record` before `delete` is legal and stop checking the key. No array helpers — `with`/`toSorted`/`toSpliced`/`toReversed` are `ReadonlyArray` methods every supported Node ships, which is why `lib` moved to ES2023 while `target` stayed ES2022. One existing test changed and was not weakened: the audit subscriber's non-aliasing case proved itself by mutating `roles` after the publish, which now throws, so it asserts non-aliasing by reference plus `isFrozen` — a regression in either mechanism still fails it. Not claimed: no freeze in production by construction; no defensive copy per subscriber (a structured clone per delivery to prevent what should not compile); no type-aware lint rule enforcing `readonly`, which is a change to the lint gate's shape and wants its own decision; and a `Date` or `Buffer` reachable from a frozen structure is still mutable in place. Found while reading the cache path and deliberately left alone as out of scope: `UserRow` carries `password_hash` and the users routes serialise the row straight into the response envelope (PR #35)
- [x] Transactional outbox table + relay worker with at-least-once delivery — `POST /v1/users` committed a row and *then* published, which is two writes to two systems with a window between them: a process that died in that window left a user nothing downstream ever heard about, permanently, since the process that would have retried the publish is the one that died. Publishing first only inverts it into announcing a user that was rolled back. The event now lands in `outbox_messages` inside the write's own transaction, and `enqueue` takes the branded `TransactionClient` — an enqueue on the pool commits independently of the write it describes and reintroduces the bug at the point of fixing it, which no single-request test would see. That brand is also what makes an enqueue safe inside `withRetryableTransaction` where a publish is not: a retried attempt takes the outbox row back with it. What is claimed is at-least-once and nothing more. One batch is one transaction — claim with `FOR UPDATE SKIP LOCKED`, dispatch, delete, commit — so a crash between the dispatch and the commit rolls the delete back and the next relay redelivers; exactly-once across two systems that cannot commit together does not exist, and the honest arrangement is the one that never loses a message and says it may repeat one. `SKIP LOCKED` rather than the purge job's advisory lock, because a sweep wants exactly one winner and a queue wants every worker busy; claims are locks rather than an `in_flight` mark, because a dead relay's lock dies with its connection while a lease needs a clock, a recovery sweep, and can expire while its dispatcher is still running — redelivering a message concurrently with itself, which a lock cannot do. A delivered row is deleted rather than marked `published`: nothing ever reads one, and keeping them turns a queue into an unbounded log needing a second janitor, the defect PR #33 found in `idempotency_keys`. Two distinctions carry the rest. A dispatcher failure is contained per message while a store failure propagates — the first leaves the transaction healthy and one poison row must not cost the batch its outcomes, the second means the transaction is in doubt and rescheduling under it would *guarantee* the duplicate rather than risk it. And the in-process dispatcher had to work harder than it looks: `bus.publish` isolates handler failures and resolves regardless, so awaiting it would delete a durable row for an event nothing processed, which would have bought a table, a migration and a poller in exchange for the guarantee the bus already gave — it passes a per-publish `onHandlerError`, collects the failures and throws, retiring the "best-effort, at-most-once" exposure `session-revocation.subscriber.ts` documents, at the stated cost that a retry redelivers to *every* subscriber. Writing the tests found a real gap: the bus minted a fresh id per publish, so a redelivery would have been indistinguishable from a second event, taking away the only thing a consumer can deduplicate on; `PublishOptions` now takes `eventId` and `occurredAt` and the relay supplies the row's own. Verified beyond the suite against a real PostgreSQL 16.13: the migration applied, rolled back and re-applied; a rolled-back transaction left no message and a committed one left exactly its own; three concurrent relays delivered 40 messages exactly once each in 117ms with all three doing work; a failure recorded its reason and was unclaimable until `available_at` passed, then dead-lettered on its last attempt and was never claimed again; psql could neither park a row without a reason nor invent a third status (both `23514`); messages from one commit share `occurred_at`; and a delete that failed *after* a successful dispatch left the row pending and produced exactly the documented second delivery. Not claimed: no ordering beyond a rough FIFO (`seq` is assigned at INSERT, not at commit); only `user.created` is routed through the outbox, since moving `user.deleted` trades immediate best-effort revocation for delayed guaranteed revocation and that is a decision about how long a deleted account may keep minting tokens rather than a mechanical one; no dead-letter replay; no `LISTEN`/`NOTIFY`; and the dispatch timeout bounds the relay's wait, not the work. The idempotency record still commits separately from the work it guards — the outbox is the pattern that closes that, but it restructures the middleware's response boundary and wants its own item (PR #37)

## Phase 8 — Streaming & Messaging
- [x] Node streams done right: backpressure-aware CSV ingest with `pipeline()` — `POST /v1/users/import` reads an arbitrarily large CSV off the socket at a memory cost that does not grow with the upload. Backpressure lives in exactly one place and the rest of the chain only forwards it: `createAsyncSink` withholds its write callback until the database answers, so `write()` returns `false`, the batcher stops pushing, the parser stops being pulled, the socket stops being read, and the receive window closes on the client — nothing polls and no stage knows about any other. The version that looks identical and is not is `req.on('data', () => void insert(...))`, which never returns `false` to anybody: bytes keep arriving at line rate while inserts queue, so it works on a 2 MB fixture and takes the process out on a 2 GB upload with the failure landing as an allocation error somewhere unrelated (`async` makes it worse — the promise is discarded and every rejection is unhandled). The claim is *measured*: a source counts the bytes it is asked for and the sink records how far that counter moves **while a write is awaiting**, which plateaus at ~272 KiB and is identical for a 700 KB document and a 1.4 MB one. The obvious measurement — how much had been read when the first write *began* — was in this branch first and is not a backpressure test at all: it is small whether or not anything throttles, because the first batch is assembled from the first few chunks either way, and an unbounded `highWaterMark` passes it while failing the two that shipped. That object-mode default is itself load-bearing — 16 *objects* of `batchObjects(500)` is 8,000 rows, most of the buffering the design exists to avoid, reintroduced by a default nobody typed. `pipeline()` and never `.pipe()`, for three distinct incidents: `.pipe()` does not forward errors (the destination never ends and the request hangs until a proxy times it out), does not destroy the chain (a failed sink leaves the source flowing and still consuming bandwidth), and does not clean up on premature close — which here is the ordinary case, since a client hanging up mid-upload is `ERR_STREAM_PREMATURE_CLOSE` and a real outcome rather than a bug. The parser is a character state machine and not a line splitter, because a CSV record is not a line: a quoted field may hold the delimiter, the quote and newlines, and splitting on `\n` first destroys the information needed to reassemble it. It keeps **no raw text buffer at all** — the state persists across `_transform` calls — so one fixture is re-parsed at every chunk size from 1 byte upward and must give identical records; `StringDecoder` rather than `chunk.toString()`, which silently loses a multi-byte character split across TCP segments on some payloads at some sizes and therefore survives every single-chunk fixture; and `maxRecordChars`, because a file containing no delimiter is otherwise one string the size of the upload — the hole every `buffer += chunk` splitter has, and one `highWaterMark` does not close, since it bounds what the stream buffers for you rather than what the transform chooses to keep. Two classes of failure, deliberately split: a malformed *document* aborts with a 4xx through a registered `csvErrorTranslator` (no row could then be read correctly), while a malformed *row* is collected and the import continues, because aborting on the first bad address turns a 30,000-row upload into 30,000 round trips to find 12 bad rows. Counts stay exact and the error list is capped — the wrong file rejects *every* row, and a per-row explanation for a million of them is an out-of-memory on the error path. The answer is one JSON body at the end and not streamed progress: the head goes out with the first byte, so an unbindable header could no longer be a 400 and would become a 200 whose body says something went wrong, which every generated client reads as success. Not atomic, and that is the trade — one transaction spanning the upload would pin the cluster's `xmin` horizon for the length of an arbitrarily long HTTP request, so each batch commits alone and every row goes in `ON CONFLICT (email) DO NOTHING`, which makes re-uploading the file the correct response to any failure. No `Idempotency-Key`, for the reason it cannot have one: the middleware fingerprints the body, and reading the body to hash it is the buffering this endpoint exists to avoid — the rows are idempotent, the response is not, and a retry reports `written: 0`. The route is undecorated on purpose: `withRetry` would re-invoke an operation whose `req` is already consumed, so the second attempt reads zero bytes and reports a successful import of nothing, converting a transient failure into silent data loss; `withCache` would key on a URL that says nothing about the body; `withTimeout` is a deadline on a request whose legitimate duration is set by how much the client sent. `BaseRepository.createMany` encodes the protocol's 65,535 bind-parameter ceiling, because exceeding it fails with a wire-level message naming nothing that leads back to the batch size, and requires a uniform column list rather than padding with `NULL`, which would override a column's `DEFAULT` on exactly the rows that omitted it. Writing the tests found one real defect: `req.is()` is three-valued and answers `null` — not `false` — when a request declares no body, so the media-type guard as first written accepted a bodyless request, which parses as an empty document and reports a successful import of nothing. Verified beyond the suite against a real PostgreSQL 16.13: a multi-row insert with a quoted roles list, `roles` round-tripping as `text[]`, a blank cell falling through to the `ARRAY['user']` default, a re-upload writing only the new row with no duplicates, 5,000 rows in 10 statements in 151ms, a mid-stream sink failure leaving the earlier batches committed and a re-upload resuming to exactly 10 rows, and the server itself rejecting an over-ceiling batch that bypassed the check (`bind message supplies 0 parameters, but prepared statement requires 65536`). That exercise also **retracted a claim this branch originally shipped**: the comment justifying in-batch de-duplication said `ON CONFLICT DO NOTHING` would insert an in-statement repeat twice, and it does not — 16.13 handles it by speculative insertion, and `DO UPDATE` is the form that fails with "cannot affect row a second time"; the de-duplication is kept as an optimisation (500 bind slots to insert one row) and the comment now says the true thing. All five gates green on Node 22.22.2 and 24.11.0 under `--throw-deprecation`; 1294 tests, up from 1062. Not claimed: no resumable upload protocol, no progress before the import finishes, no gzip bodies (415 rather than parsed as CSV, which would surface as a misleading over-long-record error), no `COPY FROM STDIN` fast path — faster, and it would give up the per-row validation and error reporting that are most of this endpoint's value — no invalid-UTF-8 detection, since a legitimate U+FFFD is indistinguishable from one the decoder produced, and CI still exercises none of the Postgres verification above, because `jest.setup.ts` overrides `DATABASE_URL` unconditionally (predates this item, noted in PR #33) (PR #38)
- [x] Large file streaming download with range requests + `ETag` — `GET /v1/uploads/:objectId`, and the three things in the title are one feature: a client cannot safely resume a half-finished download without a way to ask whether the file it was downloading is still the file on the other end. Four modules, and the split is what makes the range table in `docs/range-downloads.md` exhaustive rather than representative — `http/entity-tag.ts`, `http/range.ts` and `http/conditional.ts` take strings and numbers and return values, with no Express, no storage and no status codes anywhere in them, so every case is cheap to write down; `http/byte-range.ts` is the only part that holds a `Response`. The entity-tag scanner was *moved* out of `concurrency/etag.ts` rather than copied: `If-Match` on a write and `If-None-Match` on a read need different comparisons over the same grammar, and a comma is a legal character inside a tag, so the list parser is the one function nobody should own twice. The distinctions carry the behaviour. An invalid `Range` is 200 and an unsatisfiable one is 416 — RFC 9110 §14.2 requires ignoring a header that cannot be parsed, while a range that parses and names bytes that are not there is understood perfectly, and serving everything instead hands a resuming client the bytes it already has starting at the offset it asked to skip. A failed `If-Range` sends everything rather than failing, because the client said "resume if unchanged, otherwise start over" and 412 strands a client that could have restarted. `If-None-Match` is weak comparison and `If-Range` is strong, which is why every `ObjectStat.etag` is strong (a SHA-256 for the memory driver, S3's own tag passed through untouched): a weak validator means "equivalent representations", and two equivalent representations may differ byte for byte — exactly what splicing a range onto already-downloaded bytes cannot survive. `If-Modified-Since` is ignored outright when `If-None-Match` is present (§13.1.3), since requiring both to pass turns every revalidation of a touched-but-unchanged file into a full transfer. Multi-range gets 200, because answering one of several requested ranges is not permitted and `multipart/byteranges` is a boundary, a part header per range and a length computed up front, for a case no real client sends. The read is `stat` then `openRange` — a 304 and a 416 are built entirely out of metadata, and an endpoint that opened the object to produce them would transfer the thing it is about to say not to transfer — with the `stat`'s tag passed back in as `ifMatch`, so an object replaced in the window between the two calls fails instead of being served under the previous representation's `Content-Length`. Streaming is `pipeline()` and never `.pipe()`: on a disconnect `pipe` leaves the source running, which for a 40 GB S3 object is a transfer still being paid for after the recipient hung up. The S3 adapter pushes `Range` down to `GetObject`; the memory adapter yields 64 KiB chunks, because a single-chunk `Readable.from` has no second `read()` and so can never exert backpressure, which would make the driver useless for proving any of the above. Writing the tests found a real defect pre-dating this item: `buildObjectKey`'s extension was `slice(lastIndexOf('.'))`, so `fileName: "../../etc/passwd" produced the key `uploads/<uuid>./etc/passwd` — client-supplied path segments outside the prefix a bucket policy is written against, and a plain traversal on any filesystem-backed driver. Nothing was reachable through it (no such key satisfies the route's id pattern) but it was writable; the extension is now matched against the same shape the route accepts, so every key the module builds is one it can parse back, asserted both ways. A second, smaller one is pinned too: `Content-Type` and `Content-Length` were being committed before `open()`, so a backend failure left a `text/plain` head on the JSON error the handler wrote instead. All five gates green on Node 22 and 24 under `--throw-deprecation`; 1459 tests, up from 1294. Not claimed: no `multipart/byteranges`; no `Content-Disposition` (the original filename is deliberately not stored); `If-Match`/`If-Unmodified-Since` are not evaluated on reads, which stays `@/concurrency`'s to own on writes; and the S3 read path is covered against a stubbed client only, since this environment has no bucket (PR #39)
- [x] Server-Sent Events endpoint with heartbeat and `Last-Event-ID` resume — `GET /v1/events/stream`, and the three things in the title are one feature: a response that stays open needs the heartbeat to survive the path it runs over, and a client that reconnects needs a cursor that means something, because a resume from the wrong place is worse than a failure. Ids are `<streamId>:<sequence>` and the prefix is load-bearing: a bare sequence restarts at 1 after a deploy, so a client reconnecting with `47` asks to resume from an event that exists, is not the event it saw, and sits in the middle of the new run's history — the server replays 48 onwards and both sides believe it worked. With a per-run prefix that cursor cannot match, so the same reconnect gets an honest `reset`, which is also the right answer behind a load balancer where the log is per-replica. A cursor that cannot be honoured is answered *on an open stream* rather than with a 4xx: the request is fine and the only thing wrong is the assumption that the client's view is still incrementally reachable, so refusing would take away the stream it needs in order to recover. That is the `stream.open` control event (`live` / `replayed` / `reset` + `expired` / `unknown-stream` / `malformed`), sent ahead of the replay it describes so a client knows what the frames after it are rather than learning once it has applied them; it carries no `id`, because a frame naming no position in the log must not move a cursor onto one. A slow consumer is dropped, not buffered — `res.write()` to a peer that has stopped reading neither blocks nor fails, it buffers in-process without limit, so one stalled subscriber accumulates every event the service publishes and the incident lands as an allocation failure somewhere unrelated; `write()` returning `false` is deliberately not the trigger, since that is the ordinary state of a socket mid-burst, and `destroy()` rather than `end()` because a FIN would queue behind the backlog being escaped. Dropping is safe here in a way it is not for a request/response route, and the reason is `Last-Event-ID` itself: the client reconnects and is replayed what it missed. The wire format has no escape sequence at all, so a newline in `id` or `event` does not corrupt a field — it ends it and starts another, turning a payload into a forged event; U+0000 is rejected for a worse reason, that a client *ignores* an id containing one, so the frame arrives and the cursor silently does not advance. `data` is normalised instead, because a break there is expressible and the client's parser rejoins with LF regardless. `Cache-Control` carries `no-transform` and not only `no-cache`, since a compressing intermediary may buffer a body to compress it and a buffered event stream delivers nothing until it ends. `Last-Event-ID` has two entry points because `EventSource` maintains the header itself across a dropped connection but starts empty after a page reload and cannot be given one — the same limitation that keeps the route on bearer auth rather than `?access_token=`, which would write a live credential into every access log on the path. `server.ts` grew a shutdown handler: `server.close()` waits for open connections and this is the first route whose connections never end. 124 new tests, backpressure measured against a real `PassThrough` nobody reads from rather than a mock returning `true`, and 13 e2e cases against a real socket on an ephemeral port, because `supertest` resolves when a response ends and this one never does. Not done: the feed is an at-most-once in-process subscriber, so an event published while the process is down or on another replica never reaches this log — a reconnect after either is answered `reset` rather than papered over (PR #40)
- [x] WebSocket server (`ws`) with JWT handshake auth and per-connection rate limits — `ws://…/v1/ws` on the same `http.Server` as the REST API, since a WebSocket connection *starts* as an HTTP GET and a second listener would need its own port, TLS terminator and ingress rule to receive a handshake this one already gets. What made it more than wrapping `ws` is that a socket gives up every bound a request has by construction, and each had to be rebuilt: inbound rate (two token buckets per connection — a bucket rather than the REST routes' fixed window, because a fixed window admits twice its limit across a boundary and on a socket, where nothing waits for a response, that burst *is* the attack; and two dimensions because message count does not bound work — ten 64 KB frames a second is inside any message budget and is 640 KB/s of parsing), inbound size (`maxPayload` at reassembly, the only real bound: by the time a handler runs the whole frame is already buffered), backpressure (`send()` to a peer that stopped reading buffers in-process without limit), liveness (a peer that vanished without a FIN leaves a socket this process believes is open forever), and credential lifetime — the one most often missed, since every REST route re-checks its token and a socket checks it once, so without a close scheduled at `exp` a fifteen-minute token authorises a week-long connection. Auth is at the handshake, not a first message, so an unauthenticated peer never costs a tracked connection; the token rides the `bearer.auth.v1` subprotocol because the browser `WebSocket` constructor has no headers argument, and the two alternatives are worse — `?token=` writes a live credential into every access log and `Referer` on the path, and a cookie is what makes cross-site WebSocket hijacking possible, since a WS handshake ignores the same-origin policy and CORS does not apply to it. The server echoes the sentinel, never the token. `noServer: true` with our own `upgrade` listener, so a refusal is a real HTTP response carrying the same JSON envelope as every REST route rather than what `verifyClient` manages, which is a bare status with no body. One bug surfaced as a hung suite and is now a rule: a listener that ignores a path it does not serve *leaks* the socket — Node destroys an unhandled upgrade only when the server has no listener at all — so `server.close()` waits on it forever; unserved paths are answered 404 and an attachment owns the upgrade event for its server (PR #41)
- [x] Redis Streams consumer group worker with claim-on-stall recovery — `pnpm worker:stream`, and the two halves of the title are one feature: a group is only worth its bookkeeping over a plain `XREAD` because the pending entries list survives the consumer that was holding an entry, and the reclaim is what turns that record into recovery. There is no retry scheduler here at all, which is the design rather than an omission — a handler that throws simply does not acknowledge, the entry stays in the PEL, and its idle clock *is* the backoff; Redis already keeps one timer per entry, so a second one in this process would be two schedules for one fact. `XPENDING` + `XCLAIM` rather than `XAUTOCLAIM`, which is a round trip cheaper and reports no delivery counts: without them there is no way to tell a consumer that died holding good work from an entry that *kills* consumers — both are idle, both get claimed, and the poisoned one comes back a minute later forever — so the extra round trip buys the ceiling that stops it, and an entry that reaches it is parked on `<key>:parked` with its fields intact rather than logged and dropped. The claim repeats the idle floor the `XPENDING` already filtered on, and that repetition is the entire concurrency control: another worker may have claimed the same entry in between and reset its clock, so the server refuses per entry and two reclaimers divide the work instead of duplicating it — which is why a short claim is ordinary rather than an error, the other cause being a `MAXLEN` eviction of an entry that was still pending, the one place work is genuinely lost and the reason the cap is a safety margin rather than a queue depth (acknowledging an entry does not remove it from the stream; an untrimmed key grows for the life of the service). The number that will bite is `minIdleMs`, refused at boot when it does not exceed `handlerTimeoutMs`: an entry's idle clock starts when it is *delivered*, so a handler allowed to run for the timeout routinely leaves its entry idle that long while nothing is wrong, and a floor at or below it hands that entry to a second consumer while the first still holds it — the same work, twice, concurrently, on a healthy system, surfacing as a duplicate-processing bug rather than a configuration one. Two connections and never one, because a blocking `XREADGROUP` owns its socket for the whole block and an `XACK` behind it lands late while a shutdown cannot get through at all; and retirement is conditional, because `XGROUP DELCONSUMER` does not reassign a consumer's pending entries, it deletes them. Three defects were found by running it rather than reading it, and each shipped with the test that caught it: `call('XREADGROUP')` returns RESP3's flattened map while `ioredis`'s typed helper returns RESP2's array of pairs, so a parser written for either alone passes every fake and reads nothing in production; `enableOfflineQueue: false` is correct for a reconnect and fails the *first* connect outright, which is the group creation every consumer starts with; and a tick that fails instantly made the loop a busy wait for the length of an outage, now an interruptible jittered ladder. A fourth surfaced only in the end-to-end run — the entrypoint resolving the bus from `appContainer` was both an import cycle and a bus with no subscribers attached, delivering every entry to nobody and reporting a healthy drain — so the worker is its own composition root, like `app.ts`. `OUTBOX_DISPATCH_TARGET` defaults to `bus`, so with `REDIS_URL` unset nothing about the running service changes (PR #42)
- [ ] BullMQ job queue with retries, backoff, and a dead-letter queue

## Phase 9 — Resilience & Observability
- [ ] Circuit breaker + retry with full jitter on outbound HTTP
- [ ] Per-dependency bulkheads and hard socket/request timeouts
- [ ] Graceful shutdown: drain the server, finish in-flight requests, close the pool
- [ ] OpenTelemetry auto-instrumentation with W3C trace context propagation
- [ ] Prometheus RED metrics + a checked-in Grafana dashboard
- [ ] Health endpoints split into liveness vs readiness with dependency checks

## Phase 10 — Security Hardening
- [ ] Helmet, strict CSP, HSTS, and an env-driven CORS allowlist
- [ ] Refresh-token reuse detection with family revocation
- [ ] Field-level encryption at rest (AES-256-GCM) with envelope keys
- [ ] PII redaction in structured logs
- [ ] Request signing (HMAC) for webhooks with constant-time verification and replay windows
- [ ] OWASP API Top 10 checklist with a test per mitigation
- [ ] mTLS termination notes + client-cert verification middleware

## Phase 11 — TDD & Advanced Testing
- [ ] TDD kata: one feature built red→green→refactor, one commit per step
- [ ] Mutation testing with Stryker + CI threshold
- [ ] Property-based tests with fast-check
- [ ] Testcontainers integration tests against real Postgres
- [ ] Load test with k6 and a latency budget asserted in CI
